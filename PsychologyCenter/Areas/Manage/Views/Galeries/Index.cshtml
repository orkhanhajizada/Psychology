@model IEnumerable<PsychologyCenter.Models.Galery>

@{
    ViewBag.Title = "Galereya";
}



<p>
    @Html.ActionLink("Əlavə et", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Kategoriya
        </th>
        <th>
            Kiçik şəkil
        </th>
        <th>
           Böyük şəkil
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GaleryCategory.Name)
        </td>
        <td>
            <img src="~/Uploads/@item.Photo" width="100" />
        </td>
        <td>
            <img src="~/Uploads/@item.BigPhoto" width="100" />
        </td>
        <td>
            <a class="mr-2" href="@Url.Action("edit", new { id=item.Id })">
                <i class="fas fa-edit"></i>
            </a>
            <a class="text-danger" href="@Url.Action("delete", new { id=item.Id })">
                <i class="fas fa-trash"></i>
            </a>
        </td>
    </tr>
}

</table>
